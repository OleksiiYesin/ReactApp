pipeline {
    agent {
        kubernetes {
            yamlFile 'tempPod.yml'
        }
    }
    stages {
        stage('Main') {
            steps {
              container('shell') {
                sh 'chmod u+x ./scripts.sh'
                sh './script.sh'
              }
            }
        }
    }
}
