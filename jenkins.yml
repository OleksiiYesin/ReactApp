pipeline {
    agent {
        kubernetes {
            yamlFile 'pod_ubuntu.yml'
        }
    }
    stages {
        stage('Main') {
            steps {
              container('shell') {
                sh './script.sh'
              }
            }
        }
    }
}
