pipeline {
    agent {
        kubernetes {
          namespace 'jenkins'
        }
    }
    stages {
        stage('Deploy ReactJS') {
            steps {
              container('shell') {
                sh 'whereis kubectl'
              }
            }
        }
    }
}
