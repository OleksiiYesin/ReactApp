pipeline {
    agent {
        kubernetes
    }
    stages {
        stage('Deploy App') {
            steps {
                script {
                  kubernetesDeploy(configs: "react.yml", kubeconfigId: "kubeconfig")
                }
            }
        }
    }
}
