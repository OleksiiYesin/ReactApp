pipeline {
  agent {
   kubernetes {
    namespace 'jenkins'
   }
  }
    stages {
    
        stage('Deploy App') {
            steps {
              echo "==============DEPLOYING APPLICATION============="
              kubernetesDeploy(kubeconfigId: 'kubeconfig'){
                 sh 'kubectl create ns react'  
              }
     
            }
        }
        
        stage('Finish Stage') {
            steps {
                echo "============FINISHING SLEEP==================="
                sh 'hostname'
                sh 'sleep 200'
            }
        }        

    }
}
