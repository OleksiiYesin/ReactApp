pipeline {
  agent {
   kubernetes {
    namespace 'jenkins'
   }
  }
    stages {
    
        stage('Deploy App') {
            steps {
               script {
                 kubernetesDeploy(configs: "react.yml", kubeconfigId: "kubeconfig", enableConfigSubstitution: true)
               }     
            }
        }
        
        stage('Finish Stage') {
            steps {
                echo "============FINISHING SLEEP==================="
                sh 'hostname'
                sh 'sleep 200'
            }
        }        

    }
}
