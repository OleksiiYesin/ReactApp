pipeline {
    agent {
        kubernetes {
            yamlFile 'tempPod.yml'
        }
    }
    stages {
        stage('Main') {
            steps {
              container('shell') {
                sh './script.sh'
              }
            }
        }
    }
}
