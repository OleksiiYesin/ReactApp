apiVersion: v1
kind: Service
metadata:
  name: my-js-service
spec:
  selector:
    project: react
  ports:
    - name: app-listener
      protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
